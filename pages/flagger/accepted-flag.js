import { Column } from "rbx";
import Sidebar from "../../components/Sidebar";

const AcceptedFlag = () => {
  return (
    <Sidebar>
      <Column>
        <h1>AcceptedFlag</h1>
      </Column>
    </Sidebar>
  );
};

export default AcceptedFlag;
